#include "Pods-Sample-Selene.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Selene" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Selene"
OTHER_LDFLAGS = -ObjC ${PODS_SAMPLE_SELENE_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}